apiVersion: apps/v1
kind: Deployment
metadata:
  name: goddess
  namespace: moon
  labels:
    app.kubernetes.io/name: goddess
    app.kubernetes.io/part-of: moon
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: goddess
  template:
    metadata:
      labels:
        app.kubernetes.io/name: goddess
        app.kubernetes.io/part-of: moon
    spec:
      containers:
        - name: goddess
          image: ghcr.io/aide-family/moon/goddess:v0.0.4
          imagePullPolicy: IfNotPresent
          args:
            - run
            - all
            - -c
            - /etc/moon/config
            - --log-level=INFO
          ports:
            - name: http
              containerPort: 8000
              protocol: TCP
            - name: grpc
              containerPort: 9000
              protocol: TCP
          env:
            - name: MOON_GODDESS_JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: moon-secrets
                  key: goddess-jwt-secret
            - name: MOON_GODDESS_SERVICE_KEY_MARKSMAN
              valueFrom:
                secretKeyRef:
                  name: moon-secrets
                  key: service-key-marksman
            - name: MOON_GODDESS_SERVICE_KEY_RABBIT
              valueFrom:
                secretKeyRef:
                  name: moon-secrets
                  key: service-key-rabbit
          volumeMounts:
            - name: config
              mountPath: /etc/moon/config
              readOnly: true
            - name: data
              mountPath: /data
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 20
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: "1"
              memory: 512Mi
      volumes:
        - name: config
          configMap:
            name: goddess-config
        - name: data
          persistentVolumeClaim:
            claimName: goddess-data
