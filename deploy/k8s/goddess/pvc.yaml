apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: goddess-data
  namespace: moon
  labels:
    app.kubernetes.io/name: goddess
    app.kubernetes.io/part-of: moon
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
