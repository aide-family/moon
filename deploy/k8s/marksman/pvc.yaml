apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: marksman-data
  namespace: moon
  labels:
    app.kubernetes.io/name: marksman
    app.kubernetes.io/part-of: moon
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
